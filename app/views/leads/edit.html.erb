<% content_for :title, "Edit Lead" %>

<div class="space-y-6">
  <!-- Page Header -->
  <div>
    <h1 class="text-3xl font-bold text-gray-900">Edit Lead</h1>
    <p class="text-gray-600 mt-2">Update <%= @lead.name %>'s information</p>
  </div>

  <!-- Form Card -->
  <div class="card">
    <%= render "form", lead: @lead %>
  </div>
</div>
